package com.github.infrastructure.core.service

/**
 * Marker package for the `service` layer of the core module.
 *
 * The core module ships shared cross-cutting services (response envelope,
 * exception handling, web auto-configuration) and does not currently expose
 * any business service of its own. This file keeps the package present so
 * the module layout stays consistent with the other modules.
 */
internal object CoreServicePackageMarker
