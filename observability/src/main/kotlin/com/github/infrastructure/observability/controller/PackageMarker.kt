package com.github.infrastructure.observability.controller

/**
 * Marker package for the `controller` layer of the observability module.
 *
 * The observability module currently ships the trace-id filter and
 * auto-configuration only and does not expose HTTP endpoints of its own.
 * This file keeps the package present so the module layout stays
 * consistent with the other modules.
 */
internal object ObservabilityControllerPackageMarker
