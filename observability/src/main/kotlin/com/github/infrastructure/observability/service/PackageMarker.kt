package com.github.infrastructure.observability.service

/**
 * Marker package for the `service` layer of the observability module.
 *
 * The observability module ships the trace-id filter and auto-configuration
 * and does not currently expose any business service of its own. This file
 * keeps the package present so the module layout stays consistent with the
 * other modules.
 */
internal object ObservabilityServicePackageMarker
